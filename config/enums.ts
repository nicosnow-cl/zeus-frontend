export enum Locale {
  Es = 'es',
  En = 'en',
}

export enum Routes {
  Root = '/',
  Home = '/home',
  SignIn = '/sign-in',
  SignUp = '/sign-up',
  Contact = '/contact',
  About = '/about',
  Blog = '/blog',
}
