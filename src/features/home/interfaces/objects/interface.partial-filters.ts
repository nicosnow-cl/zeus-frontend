// import EscortType from '../../types/type.escort';

export interface IPartialFilters {
  appareance?: string[]
  city?: string
  name?: string
  promotion?: boolean
  services?: string[]
  // type?: '' | EscortType;
  video?: boolean
}
